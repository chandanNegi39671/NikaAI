# ─────────────────────────────────────────────────────────────────────────────
# Nika AI — Backend Dependencies
# Sprint 1: Core API + YOLOv8 Inference
#
# Install:  pip install -r requirements.txt
# Note:     For AMD GPU (ROCm), replace torch with the ROCm wheel from PyPI.
#           For CUDA, use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.7          # Required for UploadFile support in FastAPI

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4         # BaseSettings with .env file support

# ── Computer Vision & ML ──────────────────────────────────────────────────────
ultralytics==8.2.50              # YOLOv8 model loading and inference
opencv-python-headless==4.10.0.84 # Image processing (no GUI deps)
Pillow==10.4.0                   # PIL Image for image manipulation

# ── PyTorch (CPU fallback — swap for GPU wheel in production) ─────────────────
torch==2.3.1
torchvision==0.18.1

# ── Model Hub ─────────────────────────────────────────────────────────────────
# huggingface_hub==0.23.4        # Optional: for manual model download scripts only
                                  # (PredictionService no longer auto-downloads)

# ── HTTP Client ───────────────────────────────────────────────────────────────
httpx==0.27.0                    # Async HTTP client (testing + future Gemma client)

# ── Database & PDF Reporting ──────────────────────────────────────────────────
SQLAlchemy==2.0.31
reportlab==4.2.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
